export interface ServiceItem {
  image: string;
  title: string;
  description: string;
}

export interface FormData {
  name: string;
  email: string;
  message: string;
}

export interface ReviewItem {
  name: string;
  subtitle: string;
  image: string;
  text: string;
  rating: number;
} 